---
# Modern Ansible inventory in YAML format
all:
  children:
    juniper:
      hosts:
        vMX:
          ansible_host: 192.168.1.34
          ansible_network_os: junipernetworks.junos.junos
          ansible_connection: ansible.netcommon.netconf
          ansible_port: 830
          ansible_user: "{{ vault_juniper_user }}"
          ansible_password: "{{ vault_juniper_password }}"
        vQFX:
          ansible_host: 10.80.38.151
          ansible_network_os: junipernetworks.junos.junos
          ansible_connection: ansible.netcommon.netconf
          ansible_port: 830
          ansible_user: "{{ vault_juniper_user }}"
          ansible_password: "{{ vault_juniper_password }}"
      vars:
        ansible_python_interpreter: "{{ ansible_playbook_python }}"

    linux:
      hosts:
        # centmaster:
        #   ansible_host: 192.168.1.251
        # centcmd:
        #   ansible_host: 192.168.1.242
      vars:
        ansible_connection: ssh
        ansible_user: "{{ vault_linux_user }}"
        ansible_password: "{{ vault_linux_password }}"